<!DOCTYPE html>
<html>

<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="container p-5">
    <h2>Your Cart</h2>
    <a href="/" class="mb-3 d-block">← Back to Shop</a>
    <table class="table">
        <tr>
            <th>Item</th>
            <th>Qty</th>
        </tr>
        <% cart.items.forEach(item=> { %>
            <tr>
                <td>
                    <%= item.name %>
                </td>
                <td>
                    <%= item.quantity %>
                </td>
            </tr>
            <% }) %>
    </table>
    <form action="/checkout" method="POST"><button class="btn btn-success btn-lg">Checkout</button></form>
</body>

</html>